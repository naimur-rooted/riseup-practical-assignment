# Copy to .env and fill in real values. NEVER commit the real .env.
API_KEY=change-me-to-a-long-random-string
# Load the extension unpacked once, copy its ID here, then restart the backend.
EXTENSION_ID=REPLACE_WITH_EXTENSION_ID
# Phase 3 will use these:
AIRTABLE_TOKEN=
AIRTABLE_BASE_ID=
AIRTABLE_TABLE_NAME=
