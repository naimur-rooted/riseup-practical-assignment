Steps I verified locally: I parsed cli-self-update.xmind with a Python script (python xmind_tree2.py) and checked every command, path, and URL quoted below against its nodes. None of the runbook commands themselves (mycli, curl, sha256sum, gpg, tar, mv) were executed locally.
Steps taken from research or AI: The design, command set, failure modes, and recovery pairs come from the XMind map's nodes; the runbook prose, structure, and the reflection below were drafted with AI assistance, staying strictly within what the map specifies.
Changes made after verification: Aligned retry wording to the map's exponential backoff (3 retries at 1s, 2s, 4s, 8s) instead of a generic 2-second start; used the map's staging path /tmp/mycli-update/v1.2.3/ and `sha256sum -c SHA256SUMS --ignore-missing` rather than a bare digest comparison; added the map's pre-swap probe `/opt/mycli.new/bin/mycli --version`.
Least confident step and why: Smoke Test — the map defines `mycli --health` and treats any nonzero exit as failure, but it never states the exact healthy stdout, so I cannot quote the expected success text. RELEASE_KEY_ID is likewise an unresolvable placeholder, so the GPG pinning step cannot be validated end-to-end.
